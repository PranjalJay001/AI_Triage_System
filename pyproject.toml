[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-triage-system"
version = "1.0.0"
description = "AI Triage System for OpenEnv Hackathon"
dependencies = [
    "gradio",
    "pydantic",
]
